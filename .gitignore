# LaTeX build artifacts -- never commit these; only .tex/.sty sources and the
# final compiled .pdf belong in git. (These exact files were previously
# committed by accident alongside a broken deck and had to be cleaned up.)
*.aux
*.log
*.out
*.nav
*.snm
*.toc
*.fls
*.fdb_latexmk
*.synctex.gz
*.vrb
*.bbl
*.blg
